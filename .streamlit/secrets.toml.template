# Copy this file to secrets.toml and fill in your values.
# secrets.toml is gitignored — never commit it.

# ── Anthropic API (Screen 6: Semantic CMDB Search) ────────────────────────────
[anthropic]
api_key = "sk-ant-..."        # From console.anthropic.com

# ── Screen 6 access control ───────────────────────────────────────────────────
[app]
access_key = "your-passphrase"   # Share selectively with interviewers
                                  # Leave empty ("") to disable the gate (local dev)
max_calls  = 20                   # Max AI queries per browser session (default 20)
                                  # Reload the page to reset the counter

# ── ServiceNow PDI (auto-detect live data source) ─────────────────────────────
[servicenow]
url      = "https://dev342173.service-now.com"
username = "admin"
password = "your_pdi_password"
